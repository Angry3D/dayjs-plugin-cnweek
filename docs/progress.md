# 当前进度

最后更新：2026-05-30
语系：cn

本文档只追踪当前正在推进的阶段。已完成阶段应作为独立文件归档到 `docs/archive/`。

## 当前目标

推进 `TODO-0015`：优化大跨度 `week` 归一化并补充 54 周年份测试。范围只包含 400 年周期跳跃优化和测试补强，不迁移运行时测试到 TypeScript。

## 关联待办

- `TODO-0015` 优化大跨度 week 归一化并补 54 周测试

## 当前计划

1. 在 `normalizeCnWeekTarget` 中使用 400 年周期跳跃，避免极大 week 值按年份长循环。
2. 保留现有 Day.js 年份加减行为，尤其注意闰日被裁剪的兼容表现。
3. 补充 54 周年份测试。
4. 补充更大跨度 week 和闰日回归测试。
5. 运行验证并记录结果。

## 任务拆解

| 任务                      | 状态 | 备注                                                      |
| ------------------------- | ---- | --------------------------------------------------------- |
| 登记 `TODO-0015` 当前阶段 | done | 已明确只处理 400 年周期优化和 54 周测试。                 |
| 实现 400 年周期跳跃       | done | 已使用 400 年 / 21215 周周期，减少极大 week 循环次数。    |
| 补充 54 周年份测试        | done | 已覆盖 2012 年第 54 周。                                  |
| 补充大跨度和闰日回归测试  | done | 已覆盖 100000 / -100000 week 与 2024-02-29 年份裁剪行为。 |
| 执行验证并沉淀结论        | done | 已运行 `pnpm run verify` 与 `git diff --check`。          |

## 状态

done

## 决策记录

- 使用 400 年公历周期优化 `normalizeCnWeekTarget`。
- 运行时测试继续使用 JS，TypeScript 类型由 `test/types.ts` 覆盖。
- 本阶段不改变公开 API 或 `parseInt` 兼容语义。

## 阻塞项

暂无。

## 验证

- `pnpm run verify`
- `git diff --check`
- Jest 测试从 27 条增加到 33 条，覆盖 54 周年份、10 万周跨度和闰日回归

## 下一步

等待用户确认 `TODO-0008` 与 `TODO-0015` 是否归档，并进入最终发布前总检查。
