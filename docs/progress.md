# 当前进度

最后更新：2026-05-30
语系：cn

本文档只追踪当前正在推进的阶段。已完成阶段应作为独立文件归档到 `docs/archive/`。

## 当前目标

推进 `TODO-0008`：优化 README 的 npm 发布展示完整性，让 README 更像使用者文档，突出安装、用法、API 和兼容性，弱化开发与维护细节。

## 关联待办

- `TODO-0008` 优化 README 的发布展示完整性

## 当前计划

1. 归档已完成的 `TODO-0006`。
2. 移除 README 对本地 `image.png` 的依赖，改用文字和表格解释中国式周数。
3. 重写安装、快速开始、API、示例和兼容性说明。
4. 移除或弱化本地开发、发布、版本管理等维护者内容。
5. 运行验证并记录结果。

## 任务拆解

| 任务                                    | 状态 | 备注                                                                          |
| --------------------------------------- | ---- | ----------------------------------------------------------------------------- |
| 归档 `TODO-0006` 输入处理与跨年设置重构 | done | 已写入 `docs/archive/2026-05-30-cnweek-input-refactor.md` 并从 backlog 移除。 |
| 移除 README 图片依赖                    | done | 已用文字和表格解释 2023 年前几周。                                            |
| 重写使用者用法与 API                    | done | 已聚焦安装、ESM/CommonJS、获取与设置示例。                                    |
| 移除维护者细节                          | done | 已移除本地开发、发布和版本流程章节。                                          |
| 执行验证并沉淀结论                      | done | 已运行 `pnpm run verify` 与 `git diff --check`。                              |

## 状态

done

## 决策记录

- README 面向 npm 使用者，不展示 CI、发布、安全、版本流程等维护者细节。
- “中国式第几周”使用文字和表格解释，不依赖未发布到包内的本地图片。
- 保留 TypeScript、ESM、CommonJS 和 Day.js peer 版本等使用者接入信息。

## 阻塞项

暂无。

## 验证

- `pnpm run verify`
- `git diff --check`
- `npm pack --dry-run` 由 `pnpm run verify` 触发，tarball 内容包含重写后的 README

## 下一步

等待用户确认后归档 `TODO-0008`；随后进入最终发布前总检查。真实发布仍保留到全部优化完成后的最终阶段。
