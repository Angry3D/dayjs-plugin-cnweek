# v1.1.0 发布

日期：2026-05-30
语系：cn

## 目标

准备并发布 `@relaxcoder/dayjs-plugin-cnweek@1.1.0`。

## 关联待办

- TODO-0017：准备 `v1.1.0` 版本与 changelog

## 完成内容

- 将 `CHANGELOG.md` 的未发布变更整理到 `## [1.1.0] - 2026-05-30`。
- 将 `package.json` 版本号更新为 `1.1.0`。
- 提交 release prep：`c498721 chore: release v1.1.0`。
- 维护者手动推送代码和 tag，并等待 GitHub Actions Release Check。
- 维护者使用 npm staged publishing 完成云端 staged 包复核与 2FA 审批。
- 更新版本管理文档、backlog 和 progress。

## 验证

- `pnpm run format`：通过。
- `pnpm run verify`：通过。
- `git diff --check`：通过。
- `npm pack --dry-run` tarball 版本为 `1.1.0`，包含 8 个发布文件。
- 维护者确认 npm 发布成功。

## 决策记录

- 本次发布使用 `minor` 版本，因为包含 TypeScript 类型声明、ESM 入口和现代 exports 配置。
- 真实 npm 发布由维护者本人执行，Codex 未执行 `npm stage publish`、`npm publish`、tag push 或 2FA 审批。
- 本次采用 npm staged publishing，先上传 staged 包，再由维护者复核并审批上线。

## 后续事项

- 当前 backlog 清空，项目进入 idle 状态。
- 后续如继续优化，应从 `TODO-0018` 开始登记。
